name: native_download_plugin_example
description: "Demonstrates how to use the native_download_plugin plugin."
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.1.3
  path: ^1.9.0
  native_download_plugin:
    path: ../

  cupertino_icons: ^1.0.6

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true